# Архив: стратегический план GrowHelper

Статус: завершён и развёрнут до начала рефакторинга 2026-08-25.

Результат закреплён в актуальных источниках истины:

- продуктовый и архитектурный контракт — `docs/BRIEF_v2.md`;
- L0–L1 контракты — `spec/`;
- Profile roster и plugin surface — `team.yaml`;
- production topology и история deploy — `deploy_history.md`;
- observable deterministic behavior — код и regression tests.

Исходный корневой `todo.md` удалён: незакрытые checkbox отражали исторический
порядок реализации, а не актуальный backlog, и расходились с принятым
fixed-owner контрактом для запросов на изменение GrowHelper.
